<style>
  .ins-layout {
    width: 100%;
    min-width: 0;
    min-height: 0;
    gap: 8px;
  }

  .ins-header {
    flex: 0 0 auto;
    min-width: 0;
    border-bottom: 3px solid #000;
  }

  .ins-header__copy {
    min-width: 0;
  }

  .ins-eyebrow {
    overflow: hidden;
    font-weight: 700;
    letter-spacing: 1.4px;
    line-height: 1;
    text-overflow: ellipsis;
    text-transform: uppercase;
    white-space: nowrap;
  }

  .ins-heading {
    overflow: hidden;
    font-weight: 800;
    letter-spacing: -1.2px;
    line-height: 1;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .ins-heading__place {
    font-weight: 400;
  }

  .ins-source-qr {
    flex: 0 0 auto;
    width: 64px;
    height: 64px;
    overflow: hidden;
  }

  .ins-source-qr .qr-code {
    width: 60px;
    height: 60px;
  }

  .ins-board {
    display: grid;
    grid-template-columns: minmax(0, 0.8fr) minmax(0, 1.2fr);
    flex: 1 1 auto;
    width: 100%;
    min-width: 0;
    min-height: 0;
    gap: 10px;
  }

  .ins-section {
    min-width: 0;
    min-height: 0;
    overflow: hidden;
    border: 1px solid #000;
  }

  .ins-section__header {
    flex: 0 0 auto;
    min-width: 0;
    border-bottom: 1px solid #000;
  }

  .ins-section__art {
    display: block;
    width: auto;
    max-width: 58%;
    height: 44px;
    object-fit: contain;
    object-position: left center;
  }

  .ins-section__title {
    min-width: 0;
    overflow: hidden;
    font: 800 16px/1 Inter, sans-serif;
    letter-spacing: 1.2px;
    text-overflow: ellipsis;
    text-transform: uppercase;
    white-space: nowrap;
  }

  .ins-section__count {
    flex: 0 0 auto;
    font: 700 13px/1 Inter, sans-serif;
  }

  .ins-produce-list {
    display: grid;
    flex: 1 1 auto;
    min-width: 0;
    min-height: 0;
    gap: 4px 12px;
  }

  .ins-produce-list--vegetables {
    grid-template-columns: repeat(3, minmax(0, 1fr));
    grid-auto-flow: column;
  }

  .ins-produce-list--fruit {
    grid-template-columns: repeat(2, minmax(0, 1fr));
    grid-auto-flow: column;
  }

  .ins-layout--density-maximum .ins-produce-list {
    grid-template-rows: repeat(8, minmax(0, 1fr));
  }

  .ins-produce {
    display: flex;
    align-items: center;
    min-width: 0;
    overflow: hidden;
    font: 650 22px/1.05 Inter, sans-serif;
    letter-spacing: -0.25px;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .ins-category-list {
    display: grid;
    grid-auto-rows: minmax(0, 1fr);
    flex: 1 1 auto;
    min-width: 0;
    min-height: 0;
    gap: 3px;
    padding: 5px 7px;
  }

  .ins-category {
    display: flex;
    flex-direction: column;
    place-content: center;
    min-width: 0;
    overflow: hidden;
    border-bottom: 1px dotted #777;
  }

  .ins-category:last-child {
    border-bottom: 0;
  }

  .ins-category__name,
  .ins-category__examples {
    min-width: 0;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .ins-category__name {
    font: 800 18px/1.05 Inter, sans-serif;
  }

  .ins-category__examples {
    font: 500 14px/1.1 Inter, sans-serif;
  }

  .ins-empty {
    flex: 1 1 auto;
    width: 100%;
    min-height: 0;
    border: 2px solid #000;
    gap: 8px;
  }

  .ins-empty__mark {
    width: 54px;
    height: 54px;
    border: 2px solid #000;
    border-radius: 50%;
    font: 800 28px/1 Inter, sans-serif;
  }

  .ins-empty__message {
    max-width: 85%;
  }

  .ins-quiet {
    flex: 1 1 auto;
    width: 100%;
    min-height: 0;
    border: 1px solid #000;
    gap: 5px;
  }

  .ins-layout--half-horizontal,
  .ins-layout--quadrant {
    gap: 5px;
  }

  .ins-layout--half-horizontal .ins-header,
  .ins-layout--quadrant .ins-header {
    border-bottom-width: 2px;
    letter-spacing: -0.6px;
  }

  .ins-layout--half-horizontal .ins-eyebrow,
  .ins-layout--half-vertical .ins-eyebrow {
    letter-spacing: 1px;
  }

  .ins-layout--half-horizontal .ins-board {
    grid-template-columns: minmax(0, 0.75fr) minmax(0, 1.25fr);
    gap: 7px;
  }

  .ins-layout--half-horizontal .ins-section__title,
  .ins-layout--half-vertical .ins-section__title {
    font: 800 14px/1 Inter, sans-serif;
  }

  .ins-layout--half-horizontal .ins-section__count,
  .ins-layout--half-vertical .ins-section__count {
    font: 700 11px/1 Inter, sans-serif;
  }

  .ins-layout--half-horizontal .ins-section__header {
    padding: 3px 6px !important;
  }

  .ins-layout--half-horizontal .ins-category-list {
    gap: 1px;
    padding: 3px 7px;
  }

  .ins-layout--half-horizontal .ins-category {
    flex-direction: row;
    align-items: baseline;
    gap: 5px;
  }

  .ins-layout--half-horizontal .ins-category__name {
    flex: 0 0 auto;
    font: 800 16px/1.05 Inter, sans-serif;
  }

  .ins-layout--half-horizontal .ins-category__name::after {
    content: " ·";
  }

  .ins-layout--half-horizontal .ins-category__examples {
    font: 500 13px/1.1 Inter, sans-serif;
  }

  .ins-layout--half-vertical .ins-board {
    grid-template-columns: minmax(0, 0.9fr) minmax(0, 1.1fr);
    gap: 7px;
  }

  .ins-layout--half-vertical .ins-category__name,
  .ins-layout--half-vertical .ins-category__examples {
    overflow: visible;
    text-overflow: clip;
    white-space: normal;
  }

  .ins-layout--quadrant .ins-heading {
    letter-spacing: -0.4px;
  }

  .ins-layout--quadrant .ins-eyebrow {
    letter-spacing: 0.8px;
  }

  .ins-layout--quadrant .ins-board {
    grid-template-columns: minmax(0, 0.9fr) minmax(0, 1.1fr);
    gap: 5px;
  }

  .ins-layout--quadrant .ins-section__title {
    font: 800 13px/1 Inter, sans-serif;
    letter-spacing: 0.8px;
  }

  .ins-layout--quadrant .ins-section__count {
    font: 700 9px/1 Inter, sans-serif;
  }

  .ins-layout--quadrant .ins-category__name {
    overflow: visible;
    font: 800 14px/1.05 Inter, sans-serif;
    text-overflow: clip;
    white-space: normal;
  }

  .ins-layout--quadrant .ins-category__examples {
    overflow: visible;
    font: 500 11px/1.1 Inter, sans-serif;
    text-overflow: clip;
    white-space: normal;
  }

</style>
