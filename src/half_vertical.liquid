<div class="layout layout--col layout--stretch ins-layout ins-layout--half-vertical">
  {% if has_data %}
    <div class="ins-header">
      <div class="ins-eyebrow label label--xsmall">{{ guide_label | escape }}</div>
      <div class="ins-heading title title--small">{{ month_name | escape }} <span class="ins-heading__place">in {{ country_short_name | escape }}</span></div>
    </div>

    {% if has_items %}
      <div class="ins-board">
      <section class="ins-section rounded--small flex flex--col">
        <div class="ins-section__header p--1 flex flex--row flex--between flex--center-y">
          <span class="ins-section__title">Fruit</span>
          <span class="ins-section__count">{% if compact.half_vertical.fruits.more_count > 0 %}+{{ compact.half_vertical.fruits.more_count }} groups{% else %}{{ fruit_count }} items{% endif %}</span>
        </div>
        <div class="ins-category-list">
          {% for category in compact.half_vertical.fruits.categories %}
            <div class="ins-category">
              <div class="ins-category__name">{{ category.name | escape }}</div>
              <div class="ins-category__examples">{% for example in category.examples %}{{ example.name | escape }}{% unless forloop.last %}, {% endunless %}{% endfor %}</div>
            </div>
          {% endfor %}
        </div>
      </section>

      <section class="ins-section rounded--small flex flex--col">
        <div class="ins-section__header p--1 flex flex--row flex--between flex--center-y">
          <span class="ins-section__title">Veg</span>
          <span class="ins-section__count">{% if compact.half_vertical.vegetables.more_count > 0 %}+{{ compact.half_vertical.vegetables.more_count }} groups{% else %}{{ vegetable_count }} items{% endif %}</span>
        </div>
        <div class="ins-category-list">
          {% for category in compact.half_vertical.vegetables.categories %}
            <div class="ins-category">
              <div class="ins-category__name">{{ category.name | escape }}</div>
              <div class="ins-category__examples">{% for example in category.examples %}{{ example.name | escape }}{% unless forloop.last %}, {% endunless %}{% endfor %}</div>
            </div>
          {% endfor %}
        </div>
      </section>
      </div>
    {% else %}
      <div class="ins-quiet rounded flex flex--col flex--center text--center">
        <div class="label label--small label--inverted">A quiet month</div>
        <div class="title title--small">No common fresh harvests</div>
      </div>
    {% endif %}
  {% else %}
    <div class="ins-empty rounded flex flex--col flex--center text--center">
      <div class="ins-empty__mark flex flex--center">?</div>
      <div class="label label--small label--inverted">Setup needed</div>
      <div class="title title--small">Choose your country</div>
      <div class="ins-empty__message label label--small">{{ error_message | escape }}</div>
    </div>
  {% endif %}
</div>

<div class="title_bar">
  <span class="title">In Season</span>
  <span class="instance">{% if has_data %}{{ total_count }} now{% else %}Country required{% endif %}</span>
</div>
